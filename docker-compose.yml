version: '3'

services:
  frontend:
    build: .
    ports:
      - "80:3000"
    command: serve -s build -l 3000

  backend:
    image: node:18
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    command: npx json-server --watch data/db.json --port 8000 --cors
